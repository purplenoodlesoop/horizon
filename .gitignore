# Dart
.dart_tool/
build/

# Environment
.env

# Vault (personal data) — anchored at repo root, so lib/src/vault/ is NOT ignored
/vault/

# Test vaults (created on demand by acceptance runs)
/phase*_vault/
/*-centralized_vault/
/centralized_vault/
/federated_vault/
/blackboard_vault/
/semi_centralized_vault/

# Runtime logs
/logs/

# macOS
.DS_Store

# Cloudflare Wrangler (deploy state, npx node_modules cache)
.wrangler/
node_modules/

# Claude Code local settings (per-machine permission grants)
.claude/settings.local.json
result
